class ChatConstants {
  // Number of chats per page to load in pagination.
  static const int chatPageSize = 20;

  // Number of messages to load per fetch in pagination.
  static const int messagePageSize = 30;
}
